<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ic_launcher"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".DealerCreateGameActivity" >

    <TextView
        android:id="@+id/ipAddressText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/startGameButton"
        android:layout_centerHorizontal="true"
        android:textColorHint="#000"
        android:textSize="48sp" />

    <TextView
        android:id="@+id/winnerTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/startGameButton"
        android:layout_below="@+id/startGameButton"
        android:layout_marginTop="22dp"
        android:text="Connected Players:"
        android:textColorHint="#000000"
        android:textSize="36sp" />

    <TextView
        android:id="@+id/connectedUsersTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/winnerTextView"
        android:layout_alignBottom="@+id/winnerTextView"
        android:layout_toRightOf="@+id/winnerTextView"
        android:text="TextView"
        android:textColorHint="#000000"
        android:textSize="36sp" />

    <Button
        android:id="@+id/startGameButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="@string/startGame"
        android:textSize="72sp" />

</RelativeLayout>
